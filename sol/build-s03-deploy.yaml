# redirect the build location to an install prefix
#   which should match the osp shortname in "osp target"
spack:
  config:
    install_tree:
      root: /share/Apps/ice24v1
  modules:
    default:
      roots:
        lmod: /share/Apps/ice24v1/lmod
      arch_folder: true
